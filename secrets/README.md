# Local secrets

Run `make setup` to create the required password files and a local self-signed TLS
certificate. Files in this directory are intentionally ignored except for this README
and the safe `.example` templates. Never commit the generated files.
